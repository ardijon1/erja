# خلاصه فارسی — سایت اختصاصی نماینده فروش بیمه عمر (پروژه Ardalan)

## موضوع
بر اساس فایل «طرح سایت نماینده فروش بیمه عمر»، این پروژه یک سایت شخصی و ابزار فروش برای یک نماینده بیمه عمر است — نه فقط صفحه معرفی، بلکه ابزاری برای جذب سرنخ، پاسخ‌گویی سریع به بیمه‌شده‌ها، و ساخت شبکه ارجاعی.

## فرض کلیدی که در معماری لحاظ شد
با توجه به این‌که قیمت‌گذاری به‌شکل «بسته» (بسته نماینده فروش) نوشته شده، فرض کردم این یک **محصول قابل تکرار** برای چند نماینده مختلف است، نه فقط یک پروژه یک‌باره. به همین دلیل تمام محتوای مخصوص مشتری (نام، عکس، آمار، سؤالات متداول) در فایل‌های `content/` نگه‌داری می‌شود، نه داخل کد — یعنی برای مشتری بعدی فقط محتوا عوض می‌شود، نه کامپوننت‌ها. اگر این فرض درست نیست و فقط یک سایت یک‌باره مدنظر است، به من بگو تا این جداسازی را ساده‌تر کنم.

## پشته فنی انتخاب‌شده
- Next.js 15 + TypeScript، میزبانی روی سرور Node.js (نه Vercel/Edge) — چون در طرح روی «زیرساخت پایدار داخل ایران» تأکید شده
- shadcn/ui برای کامپوننت‌ها و توکن‌های طراحی، Tailwind فقط برای چیدمان
- فونت وزیرمتن، حالت تاریک/روشن، راست‌چین کامل با CSS منطقی (logical properties)
- Prisma + SQLite برای داده‌ها (سرنخ‌ها، معرف‌ها، کلیک‌ها) — سبک و بدون نیاز به دیتابیس جدا
- **بدون هوش مصنوعی در بخش پرسش و پاسخ** — دقیقاً طبق تأکید طرح، همه پاسخ‌ها از پیش توسط خود نماینده نوشته می‌شود
- بدون هیچ سرویس خارجی با کلید API — QR کد، vCard و لینک واتساپ همه محلی ساخته می‌شوند

## صفحات اصلی
۱. صفحه اصلی (پروفایل + ماشین‌حساب + پیش‌نمایش پرسش‌وپاسخ + دعوت به ارجاع)
۲. کارت دیجیتال فشرده برای اسکن QR (`/card`)
۳. صفحه کامل پرسش و پاسخ (`/faq`)
۴. لینک ارجاع اختصاصی (`/r/کد`) — فقط ثبت منبع سرنخ و ریدایرکت
۵. ورود ادمین و داشبورد (جدول برترین معرف‌ها + لیست سرنخ‌ها)

## نکته حقوقی مهم که در کد پیاده‌سازی نشده
طبق تذکر خود طرح، هرگونه پاداش مالی برای معرفی باید قبلاً با بیمه مرکزی و شرکت بیمه هماهنگ شود. کد فقط «ردیابی» را انجام می‌دهد؛ هیچ منطق پاداش/تخفیف در کد وجود ندارد.

## آماده برای شروع فاز ۱
طبق `PROMPT.md`، توسعه به‌صورت پایین‌به‌بالا (زیرساخت → داده → یکپارچه‌سازی → منطق → فرانت‌اند) پیش می‌رود. فایل وایرفریم موبایل برای تمام صفحات هم همراه فایل‌های مستندات تحویل داده شده.
